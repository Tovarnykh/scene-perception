# AGENTS

Guidance for coding agents working in this repository:

- Preserve the lightweight project structure.
- Keep tests independent from external model downloads.
- Place sample input files under `data/samples/`.
- Place generated JSON under `artifacts/json/`.
- Place generated images under `artifacts/images/`.
